# Owners of the control plane (AGENTS.md R-19).
#
# Owner: Markus Ertel (@markus-ertel), TOPACA AI Labs (@topaca-ai-labs).
# Copyright © 2026, see LICENSE. All code and all texts in this repository are by
# the same author.
#
# @topaca-ai-labs is the organization. GitHub accepts only persons and teams as
# owners in this file, never an organization, so organization-wide ownership needs
# a team (for example @topaca-ai-labs/<team>). None is listed here because none
# exists yet — an invented team handle would silently point at nothing.
#
# State of the settings a file cannot set (R-19): branch protection on `main` is
# enabled — three required status checks, pull request before merging, no forced
# pushes, no deletion. "Require review from code owners" is still off, because the
# only code owner is also the author and the flag would then block that author's
# own merges; it goes on together with the team.
#
# The path list is the same list as CONTROL_PLANE in scripts/check.mjs.

*                                       @markus-ertel
/AGENTS.md                              @markus-ertel
/CLAUDE.md                              @markus-ertel
/scripts/                               @markus-ertel
/tests/                                 @markus-ertel
/.github/                               @markus-ertel
/package.json                           @markus-ertel
/package-lock.json                      @markus-ertel
/.node-version                          @markus-ertel
/astro.config.mjs                       @markus-ertel
/src/content.config.ts                  @markus-ertel
/src/lib/route.mjs                      @markus-ertel
/src/lib/site-schema.mjs                @markus-ertel
